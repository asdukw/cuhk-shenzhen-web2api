# cuhk-shenzhen-web2api

CUHK-Shenzhen AI chat (`https://ai.cuhk.edu.cn/chat/`) as a local HTTP API.

- Agent notes: `AGENTS.md`
- Local cron / scripts: `docs/local-handoff.md`

Do not commit `.env`. Copy `.env.example` and fill `FIRECRAWL_API_KEY` / `WEB2API_API_KEY`.
